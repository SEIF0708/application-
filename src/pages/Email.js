import React from 'react';
import { Typography, Box } from '@mui/material';

export default function Email() {
    return (
        <Box>
            <Typography variant="h4" fontWeight={700} mb={2}>Email</Typography>
            <Typography>Email management and analytics coming soon...</Typography>
        </Box>
    );
} 