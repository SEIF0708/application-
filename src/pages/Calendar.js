import React from 'react';
import { Typography, Box } from '@mui/material';

export default function Calendar() {
    return (
        <Box>
            <Typography variant="h4" fontWeight={700} mb={2}>Calendar</Typography>
            <Typography>Calendar integration coming soon...</Typography>
        </Box>
    );
} 