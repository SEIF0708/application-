import React from 'react';
import { Typography, Box } from '@mui/material';

export default function Kanban() {
    return (
        <Box>
            <Typography variant="h4" fontWeight={700} mb={2}>Kanban</Typography>
            <Typography>Kanban board coming soon...</Typography>
        </Box>
    );
} 